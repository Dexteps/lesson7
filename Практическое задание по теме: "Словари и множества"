# создадим словарь где будут записаны друзья и их город
from os import remove

my_dict = {
    'Андрей': 'Cургут',
    'Ксюша': 'Лянтор',
    'Люба': 'Москва',

}

# выводим на экран словарь
print(my_dict)

# выводим на экран значение по ключу Ксюша
print(my_dict['Ксюша'])

# добавляем две пары произвольных   в 2 вариантах в словарь
# 1 Вариант
my_dict['Ден'] = 'Мегион'
my_dict['Саша'] = 'Питер'
# выводим словарь на экран
print(my_dict)

# 2ой вариант
my_dict.update({
    'Крис': 'Калуга',
    'Ника': 'Страна Ос',
})
# выводим на экран словарь
print(my_dict)

# Удаляем одну пару из словаря и выводим ее на экран значение ее а потом и обновленный словарь
print(my_dict.pop('Андрей'))
print(my_dict)



# создадим множество (set) коллукцию уникальных значений
my_set = {1, 2, 3, 3, 5, 5, 1, 6, 'a', 'a', 'г', 'i ', 'i'}
# выведим на экран
print(my_set)

# добавляем 2 элемента во множество
my_set.add(9)
my_set.add(10)
# Выводим на экран
print(my_set)

# Удаление элементов множества происходит в 2х вариантах  Discard и remove
my_set.discard(1)
my_set.remove(2)
# выводим на экран
print(my_set)

# СПАСИБО ЗА ВНИМАНИЕ  =)

